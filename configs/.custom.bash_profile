#=============================================================
#=== CUSTOM .bash_profile FILE ===============================
#=============================================================

cd ~
if [[ -f ~/.bashrc ]]; then
  source ~/.bashrc
fi
